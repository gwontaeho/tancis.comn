import React from 'react'

type TreeSchemaType = any[]
type TreeDefaultSchemaType = { id: string; schema: TreeSchemaType }
type useTreeProps = { defaultSchema: TreeDefaultSchemaType }

export const useTree = (props: useTreeProps) => {
    return {}
}
