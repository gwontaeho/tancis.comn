import { Routes, Route } from "react-router-dom";
import { MainLayout } from "@/comn/layouts/MainLayout";

export const Main = () => {
    return (
        <MainLayout>
            <Routes></Routes>
        </MainLayout>
    );
};
