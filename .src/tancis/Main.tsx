import { Routes, Route } from "react-router-dom";
import { MainLayout } from "@/com/layouts/MainLayout";

export const Main = () => {
    return (
        <MainLayout>
            <Routes></Routes>
        </MainLayout>
    );
};
